local gui = Instance.new("ScreenGui"), gui.Name = "MyGUI", gui.Parent = game.Players.LocalPlayer.PlayerGui, local frame = Instance.new("Frame"), frame.Size = UDim2.new(0, 200, 0, 100), frame.Position = UDim2.new(0.5, -100, 0.5, -50), frame.BackgroundColor3 = Color3.new(0.5, 0.5, 0.5), frame.Parent = gui, local label = Instance.new("TextLabel"), label.Size = UDim2.new(1, 0, 0.5, 0), label.Position = UDim2.new(0, 0, 0, 0), label.Text = "Click the button!", label.Parent = frame, local button = Instance.new("TextButton"), button.Size = UDim2.new(1, 0, 0.5, 0), button.Position = UDim2.new(0, 0, 0.5, 0), button.Text = "Click Me!", button.Parent = frame, local function onClick(), label.Text = "Button clicked!", end, button.MouseButton1Click:Connect(onClick)
